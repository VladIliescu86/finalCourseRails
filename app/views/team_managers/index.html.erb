<p id="notice"><%= notice %></p>

<h1>Manager</h1>

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Team</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @team_managers.each do |manager| %>
    <tr>
      <td><%= manager.name %></td>
      <td><%= manager.team.name %></td>
<!--      <td><%#= link_to 'Show', team_team_manager_path %></td>-->
      <td><%= link_to 'Edit', edit_team_path(manager) %></td>
<!--      <td><%#= link_to 'Destroy', manager, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Manager', teams_path %>
